{
  "name": "@hedgedoc/dev-reverse-proxy",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0",
  "scripts": {
    "start:dev": "/opt/homebrew/bin/caddy run || ./caddy run || caddy run",
    "start": "/opt/homebrew/bin/caddy run || ./caddy run || caddy run"
  }
}
